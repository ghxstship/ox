# OX web — environment.
# Base URL of the OX Platform API (apps/api). When unset or unreachable, every
# surface falls back to local seed data (lib/seed.ts) so the app renders
# standalone. Example: https://api.ox.fit/v1
NEXT_PUBLIC_OX_API_URL=

# Supabase (public — safe in the browser; RLS does the filtering)
NEXT_PUBLIC_SUPABASE_URL="https://xaepcwnqjwphuwvuekfb.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_ZzRzJgf-NQ3g5sR6i48m8g_HbMD_DMM"
