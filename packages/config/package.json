{
  "name": "@ox/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript / lint config presets for the OX monorepo.",
  "files": [
    "tsconfig.base.json",
    "tsconfig.react.json",
    "tsconfig.node.json"
  ]
}
